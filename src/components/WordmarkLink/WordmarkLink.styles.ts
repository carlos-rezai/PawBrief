import styled from "styled-components";
import { Link } from "react-router-dom";

export const WordmarkAnchor = styled(Link)`
  display: inline-flex;
  align-items: center;
  text-decoration: none;
`;
